<p>Welcome <%= @user.email %>!</p>
<p>You can confirm your account email through the link below:</p>
<% if Rails.env.production? %>
  <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token, host: 'mysite-jjbq.onrender.com') %></p>
<% else %>
  <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token, host: 'localhost') %></p>
<% end %>
